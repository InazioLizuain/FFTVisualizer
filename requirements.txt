# Core dependencies for FFT Audio Visualizer
numpy>=1.21.0
scipy>=1.7.0

# Audio processing
sounddevice>=0.4.4

# Adafruit RGB Matrix library for LED display
# NOTE: rgbmatrix is not installed via pip. See INSTALL.md for instructions.
# rgbmatrix>=0.0.1

# GPIO and hardware interfaces
RPi.GPIO>=0.7.1
adafruit-circuitpython-busdevice>=5.0.0

# Menu and display - Character LCD
RPLCD>=1.3.0

# Menu and display - OLED (SSD1306-compatible) + drawing
adafruit-circuitpython-ssd1306>=2.12.0
pillow>=10.0.0

# Menu input - I2C rotary encoder (Adafruit Seesaw)
adafruit-circuitpython-seesaw>=1.16.0

# Configuration
pyyaml>=6.0
